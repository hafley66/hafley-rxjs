import{at as e,it as t,t as n,vt as r}from"./chunks/framework.CXXr-9tD.js";var i=JSON.parse(`{"title":"Interactive proof","description":"","frontmatter":{},"headers":[],"relativePath":"proof.md","filePath":"pages/proof.md"}`),a={name:`proof.md`};function o(n,i,a,o,s,c){return r(),t(`div`,null,[...i[0]||=[e(`<h1 id="interactive-proof" tabindex="-1">Interactive proof <a class="header-anchor" href="#interactive-proof" aria-label="Permalink to “Interactive proof”">​</a></h1><p>The large sequence opens in the document renderer. Switch to Cytoscape for native actors, lifelines, groups, notes, and 125 message edges. SVG geometry is measured during import; the native sequence retains no full SVG diagram in the DOM. The D2 architecture also has native bindings: 51 connections, with object IDs and scoped endpoint relations recovered from D2 SVG metadata. The paired examples exercise the same 3 actors, 4 messages, and 2 nested groups through D2 and Mermaid ingestion.</p><iframe src="/hafley-rxjs/grapht/proof/index.html" style="width:100%;height:calc(100vh - 10rem);border:0;" title="grapht interactive proof"></iframe><h2 id="controls" tabindex="-1">Controls <a class="header-anchor" href="#controls" aria-label="Permalink to “Controls”">​</a></h2><table tabindex="0"><thead><tr><th>Gesture or control</th><th>Behavior</th></tr></thead><tbody><tr><td>Scroll</td><td>Pan along wheel/trackpad axes</td></tr><tr><td>Shift + scroll</td><td>Horizontal pan</td></tr><tr><td>Ctrl/Cmd + scroll or trackpad pinch</td><td>Zoom at cursor</td></tr><tr><td>End wheel gesture</td><td>Damped pan/zoom momentum</td></tr><tr><td>Zoom / momentum settings</td><td>Stored sensitivity, momentum on/off, strength, decay, and maximum tail</td></tr><tr><td>Fit</td><td>Fit all, width, height, or readable text; cancels momentum. The chosen mode persists and applies on source load and resize</td></tr><tr><td>Paired examples</td><td>Equivalent D2 / Mermaid sequences, available in both renderers</td></tr><tr><td>Move actors / edges</td><td>Enable horizontal actor-lane dragging and vertical message-row dragging</td></tr><tr><td>Undo move / Redo move</td><td>Traverse the stored movement-event prefix; one drag commits one event</td></tr><tr><td>Actor ribbon / group headers</td><td>Toggle shared screen-space headers; hover uses the represented actor/group ID</td></tr><tr><td>Floating group + / −</td><td>Expand/collapse that group; keyboard Enter or Space also works</td></tr><tr><td>Hop colors</td><td>Switch between one hue with fading (default) and per-hop hues; preference persists</td></tr><tr><td>Interpolate hop colors</td><td>Blend the configurable From / To colors across the chosen hop distance; disable to use indexed palette colors</td></tr><tr><td>Dark mode (both renderers)</td><td>Change shared SVG/canvas/header colors; preference persists</td></tr><tr><td>Interaction / hover</td><td>Neighbors, upstream, downstream, both, or off; configurable depth</td></tr><tr><td>Relations</td><td>Traverse actor links or ordered sequence steps</td></tr><tr><td>Debug hover</td><td>Show logical ID, parent, endpoint IDs, source span, measured anchor, route attachment points, bindings, and incident relations</td></tr><tr><td>Group actors</td><td>Select actors and name a view group; original source stays available</td></tr><tr><td>Collapse</td><td>Hide descendants; sequence fragments compact vertically; expansion restores source geometry</td></tr><tr><td>Original source</td><td>Inspect the retained Mermaid or D2 text</td></tr></tbody></table><p>The document renderer supports text selection while movement is off. With movement enabled, actor shapes and lifelines move horizontally, keeping message endpoints attached. Message rows move vertically along the lanes. General architecture nodes move in both axes; dragging an architecture edge bends its interior route while preserving endpoint anchors. Undo/redo and the movement list persist in local storage, keyed by a fingerprint of the retained source metadata. An edited source receives a separate history. View grouping itself is not stored across reloads.</p><p>The debug inspector lets pointer gestures pass through while movement mode is enabled.</p><p>Camera, source/collapse, movement, and hover state have separate signal dependencies. Hover and camera changes do not rerun collapse geometry. The active renderer consumes derived frames and hover paint; renderer switches release the previous effect wiring and resource. SVG primitive measurement caches retain only revisions used by the current frame.</p><p>Hover is transient: it does not commit selection or movement history. Focus and first-hop neighbors use full intensity. Fading is the default. With <strong>Hop colors</strong> enabled, the default interpolation runs from amber to blue over six hops; both endpoints and distance are configurable. Disable interpolation for the preset indexed palette. Subsequent hops multiply opacity by 0.55, with unrelated context at 0.15. Edges interpolate their logical source and target colors and opacity in both modes; arrowheads use their endpoint paint. SVG uses user-space gradients, including horizontal and reversed paths. Native Cytoscape uses <a href="https://js.cytoscape.org/#style/edge-line" target="_blank" rel="noreferrer">source-to-target line gradients</a>. The pinned Cytoscape 3.34.0 patch preserves RGBA stop and arrow alpha in its canvas renderer; upstream otherwise ignores those alpha components. The patch covers the source, ESM, CJS, and unminified UMD entries used by this workspace.</p><p>Sticky actor names use the same hover origin as their in-diagram labels. Group headers show the nearest reached member&#39;s hop. Collapsed ownership groups are traversal components: their internal members share a distance, and each boundary crossing consumes one hop. This uses explicit group membership; it does not infer strongly connected components from arbitrary cycles. Parallel branches receive equal step distance and converge at their following event. Actor links and sequence steps are separate relations because repeated messages between the same actors have distinct order. Native mobile touch-pinch momentum is unverified.</p><p>Actor-group collapse hides incident messages and retains lane spacing; it does not create aggregated boundary messages. Message groups with no remaining visible content disappear recursively, including their floating headers and controls. Their empty rows compact unless a visible parallel sibling still occupies them. Groups with surviving messages or notes remain. Expanding actors restores their groups. Fragment collapse preserves a header row and compacts vertical space. View groups and collapsed state survive renderer switches and last until source replacement or page reload.</p><h2 id="shared-styles-and-layout-policy-lab" tabindex="-1">Shared styles and layout policy lab <a class="header-anchor" href="#shared-styles-and-layout-policy-lab" aria-label="Permalink to “Shared styles and layout policy lab”">​</a></h2><p>Both renderers accept <code>applyTheme(&quot;light&quot; | &quot;dark&quot; | GraphStyle)</code>. Presets and caller palettes feed one Cytoscape-compatible rule set. <code>GraphStyle.hopMode</code> accepts <code>&quot;fade&quot;</code> (default) or <code>&quot;color&quot;</code>; <code>hopColors</code> customizes the palette. The SVG adapter maps known Mermaid/D2 primitive paint properties, retaining source geometry and text metrics. Theme changes preserve the camera.</p><p>Open <strong>layout policy</strong> to try two draggable groups. Moving a child freezes automatic layout for its group. Collapse allows automatic placement of the collapsed representation. The <strong>auto layout on expand</strong> checkbox chooses automatic placement or saved manual positions on expansion. Saved positions survive either choice, and the other group keeps its own mode. The <strong>auto layout</strong> button only rearranges groups still in automatic mode. This lab uses Cytoscape&#39;s grid layout; it does not enable manual movement for the large sequence. The lab state lasts until the page ends.</p><h2 id="performance-evidence" tabindex="-1">Performance evidence <a class="header-anchor" href="#performance-evidence" aria-label="Permalink to “Performance evidence”">​</a></h2><p>The docs-kit widget consumes <code>@hafley66/trace</code> frame and memory samples. FPS means animation-frame callback delivery. Heap is a browser JS estimate; DOM counts cover descendants of the graph host. The external benchmark uses trace&#39;s OS PID RSS collector for resident memory and its Chromium collector for heap breakdowns. The hosted widget cannot read OS RSS directly.</p><p><a href="https://github.com/hafley66/hafley-rxjs/blob/main/packages/grapht/adapters/2_render_cytoscape/bench/4_report.md" target="_blank" rel="noreferrer">Benchmark and raw measurements</a>. <a href="./roadmap">Remaining interaction work</a>.</p><h2 id="stored-wheel-settings" tabindex="-1">Stored wheel settings <a class="header-anchor" href="#stored-wheel-settings" aria-label="Permalink to “Stored wheel settings”">​</a></h2><p>Open <strong>Interaction / hover → Zoom / momentum settings</strong>. Default zoom sensitivity is 1.2× the previous response. Momentum defaults remain strength 1, decay 85 ms, and a maximum 500 ms tail. Changes apply immediately to both renderers and cancel an active tail. Reset restores those defaults.</p><p>The proof reuses <code>StorageSignal</code>, <code>storageSignal(urlAdapter(&quot;wheel&quot;))</code>, and <code>sync</code> from <code>@hafley66/signals</code>. Local settings survive reloads; the <code>wheel</code> URL parameter overrides local settings when opening a shared URL. Changes replace the URL entry, preserving unrelated parameters and the hash.</p><h2 id="svg-import" tabindex="-1">SVG import <a class="header-anchor" href="#svg-import" aria-label="Permalink to “SVG import”">​</a></h2><p><strong>Open SVG</strong> accepts a local <code>.svg</code> file and optionally a <code>.json</code> file containing <code>graph</code> and <code>bindings</code>. Files stay in the browser. The source viewBox (or pixel width/height) sets the initial camera. Original source is retained. Duplicate element IDs, missing binding targets, and invalid graph references are rejected. A plain SVG opens in document mode. Native Cytoscape requires explicit semantic bindings, using the supported primitive roles; arbitrary SVG artwork remains source-rendered. The architecture fixture recovers these bindings from D2-generated object and connection identities.</p><p>DOM sequence lifelines keep a one-pixel stroke at fitted zoom. Hop palettes are shared through <code>GraphStyle.hopColors</code>, with separate light/dark defaults and the existing distance opacity.</p><h2 id="large-diagrams-and-zoom-configuration" tabindex="-1">Large diagrams and zoom configuration <a class="header-anchor" href="#large-diagrams-and-zoom-configuration" aria-label="Permalink to “Large diagrams and zoom configuration”">​</a></h2><p>Fit-all contains the source geometry without changing source orientation. The architecture source explicitly sets <code>direction: right</code> and spans 25,347 × 4,431 source units. <strong>Readable text</strong> uses a minimum scale of 0.75 and starts at the upper-left when the diagram exceeds the viewport; pan to inspect the rest. Fit width and fit height expose the other axis choices. Source layout remains a D2/Mermaid authoring choice, independent of camera fit.</p><p>Open <strong>Interaction / hover → Zoom / momentum settings</strong>. Defaults and clamping live in <code>src/lib/1_wheelCamera.ts</code>: sensitivity 1.2, momentum enabled, strength 1, decay 85 ms, maximum tail 500 ms. The proof stores these through <code>StorageSignal(&quot;grapht.proof.wheel&quot;, ...)</code> and syncs the <code>wheel</code> URL parameter in <code>adapters/2_render_cytoscape/proof/live.ts</code>. Both renderers receive the same settings.</p><p>The paired fixture sources are <code>fixtures/sequence/paired-d2.d2</code> and <code>fixtures/sequence/paired-mermaid.mmd</code>. Regenerate their measured SVG and graph bindings with <code>node scripts/3_generate_sequence_fixture.mjs paired-d2 d2</code> and <code>node scripts/3_generate_sequence_fixture.mjs paired-mermaid mermaid</code> from the Grapht package.</p><h2 id="native-labels-and-hover-paint" tabindex="-1">Native labels and hover paint <a class="header-anchor" href="#native-labels-and-hover-paint" aria-label="Permalink to “Native labels and hover paint”">​</a></h2><p>Native D2 architecture labels retain source text positions and line breaks. Their wrapping width comes from the measured SVG label bounds; container text is no longer repeated at the container center.</p><p>Hover input is coalesced once per animation frame and deduplicated by logical focus IDs. Native paint compares hop/endpoint state and changes only differing style properties. Palette RGB conversion is cached, avoiding repeated style reads that flush Cytoscape batches. Repeating an unchanged hover performs zero native style writes; unrelated actor paint remains unchanged.</p><p>Local Chromium headless measurement, 90 alternating logical hover transitions with five warm-up frames excluded, using the same fixtures and default camera/depth:</p><table tabindex="0"><thead><tr><th>Fixture</th><th style="text-align:right;">Native elements</th><th style="text-align:right;">Previous p95 frame</th><th style="text-align:right;">Updated p95 frame</th><th style="text-align:right;">Frames over 32 ms, previous → updated</th></tr></thead><tbody><tr><td>Architecture</td><td style="text-align:right;">291</td><td style="text-align:right;">70.3 ms</td><td style="text-align:right;">20.9 ms</td><td style="text-align:right;">85 → 0</td></tr><tr><td>Large sequence</td><td style="text-align:right;">444</td><td style="text-align:right;">86.3 ms</td><td style="text-align:right;">26.4 ms</td><td style="text-align:right;">85 → 0</td></tr></tbody></table><p>Timing varies by device. Reproduce with <code>node adapters/2_render_cytoscape/proof/3_hoverProbe.mjs &lt;proof-url&gt;</code> from the Grapht package. The renderer browser suite checks label placement, wrapping, unchanged-hover writes, and unrelated actor invalidation independently of timing.</p>`,33)]])}var s=n(a,[[`render`,o]]);export{i as __pageData,s as default};