import{at as e,it as t,t as n,vt as r}from"./chunks/framework.CXXr-9tD.js";var i=JSON.parse(`{"title":"Renderers","description":"","frontmatter":{},"headers":[],"relativePath":"renderers.md","filePath":"pages/renderers.md"}`),a={name:`renderers.md`};function o(n,i,a,o,s,c){return r(),t(`div`,null,[...i[0]||=[e(`<h1 id="renderers" tabindex="-1">Renderers <a class="header-anchor" href="#renderers" aria-label="Permalink to “Renderers”">​</a></h1><ul><li><a href="#the-adapter-contract">The adapter contract</a></li><li><a href="#which-adapters-exist">Which adapters exist</a></li><li><a href="#the-render-receipt">The render receipt</a></li><li><a href="#document-renderer-gestures">Document renderer gestures</a></li></ul><h2 id="the-adapter-contract" tabindex="-1">The adapter contract <a class="header-anchor" href="#the-adapter-contract" aria-label="Permalink to “The adapter contract”">​</a></h2><p>A renderer is a <code>GraphFrameResource</code>: one <code>render(frame, receipt)</code> call plus an <code>unsubscribe</code>. Declared at <code>packages/grapht/src/2_graph/10_renderer.ts:15</code>.</p><div class="language-ts"><button title="Copy code" data-copied="Copied" class="copy"></button><span class="lang">ts</span><pre class="shiki shiki-themes github-light github-dark" style="--shiki-light:#24292e;--shiki-dark:#e1e4e8;--shiki-light-bg:#fff;--shiki-dark-bg:#24292e;" tabindex="0" dir="ltr"><code><span class="line"><span style="--shiki-light:#c62739;--shiki-dark:#F97583;">type</span><span style="--shiki-light:#6F42C1;--shiki-dark:#B392F0;"> GraphFrameResource</span><span style="--shiki-light:#24292E;--shiki-dark:#E1E4E8;">&lt;</span><span style="--shiki-light:#6F42C1;--shiki-dark:#B392F0;">NodeData</span><span style="--shiki-light:#24292E;--shiki-dark:#E1E4E8;">, </span><span style="--shiki-light:#6F42C1;--shiki-dark:#B392F0;">EdgeData</span><span style="--shiki-light:#24292E;--shiki-dark:#E1E4E8;">&gt; </span><span style="--shiki-light:#c62739;--shiki-dark:#F97583;">=</span><span style="--shiki-light:#24292E;--shiki-dark:#E1E4E8;"> {</span></span>
<span class="line"><span style="--shiki-light:#6F42C1;--shiki-dark:#B392F0;">  render</span><span style="--shiki-light:#24292E;--shiki-dark:#E1E4E8;">(</span><span style="--shiki-light:#c13617;--shiki-dark:#FFAB70;">frame</span><span style="--shiki-light:#c62739;--shiki-dark:#F97583;">:</span><span style="--shiki-light:#6F42C1;--shiki-dark:#B392F0;"> GraphFrame</span><span style="--shiki-light:#24292E;--shiki-dark:#E1E4E8;">&lt;</span><span style="--shiki-light:#6F42C1;--shiki-dark:#B392F0;">NodeData</span><span style="--shiki-light:#24292E;--shiki-dark:#E1E4E8;">, </span><span style="--shiki-light:#6F42C1;--shiki-dark:#B392F0;">EdgeData</span><span style="--shiki-light:#24292E;--shiki-dark:#E1E4E8;">&gt;, </span><span style="--shiki-light:#c13617;--shiki-dark:#FFAB70;">receipt</span><span style="--shiki-light:#c62739;--shiki-dark:#F97583;">:</span><span style="--shiki-light:#6F42C1;--shiki-dark:#B392F0;"> GraphRenderReceipt</span><span style="--shiki-light:#24292E;--shiki-dark:#E1E4E8;">)</span><span style="--shiki-light:#c62739;--shiki-dark:#F97583;">:</span><span style="--shiki-light:#005CC5;--shiki-dark:#79B8FF;"> void</span></span>
<span class="line"><span style="--shiki-light:#6F42C1;--shiki-dark:#B392F0;">  unsubscribe</span><span style="--shiki-light:#24292E;--shiki-dark:#E1E4E8;">()</span><span style="--shiki-light:#c62739;--shiki-dark:#F97583;">:</span><span style="--shiki-light:#005CC5;--shiki-dark:#79B8FF;"> void</span></span>
<span class="line"><span style="--shiki-light:#24292E;--shiki-dark:#E1E4E8;">}</span></span></code></pre></div><p>The adapter owns no lifetime beyond this pair. <code>render</code> is pure projection of the current frame; the resource is torn down through <code>unsubscribe</code> when the stream ends. <code>graphRenderer</code> wraps an acquire function into an RxJS operator (<code>src/2_graph/10_renderer.ts:39</code>): it calls <code>acquire(host)</code> once per subscription and <code>resource.unsubscribe()</code> in <code>finalize</code>.</p><h2 id="which-adapters-exist" tabindex="-1">Which adapters exist <a class="header-anchor" href="#which-adapters-exist" aria-label="Permalink to “Which adapters exist”">​</a></h2><p>Every adapter is an interchangeable implementation over the same fixture graph, so their receipts are directly comparable.</p><table tabindex="0"><thead><tr><th>directory</th><th>package</th><th>lane</th></tr></thead><tbody><tr><td><code>adapters/2_render_cytoscape</code></td><td><code>@hafley66/grapht-render-cytoscape</code></td><td>render</td></tr><tr><td><code>adapters/3_render_canvaskit</code></td><td><code>@hafley66/grapht-render-canvaskit</code></td><td>render</td></tr><tr><td><code>adapters/4_render_sigma</code></td><td><code>@hafley66/grapht-render-sigma</code></td><td>render</td></tr><tr><td><code>adapters/5_render_vello_wgpu</code></td><td><code>@hafley66/grapht-render-vello-chromium</code></td><td>render</td></tr><tr><td><code>adapters/6_render_pixijs</code></td><td><code>@hafley66/grapht-render-pixijs</code></td><td>render</td></tr><tr><td><code>adapters/7_render_threejs</code></td><td><code>@hafley66/grapht-render-threejs</code></td><td>render</td></tr></tbody></table><p>Layout adapters (<code>0_layout_grid_worker</code>, <code>1_layout_grid_wasm</code>) fill <code>LayoutParticipation: managed</code> geometry rather than projecting a frame, so they sit beside, not behind, this contract.</p><h2 id="the-render-receipt" tabindex="-1">The render receipt <a class="header-anchor" href="#the-render-receipt" aria-label="Permalink to “The render receipt”">​</a></h2><p><code>GraphRenderReceipt</code> tells the adapter what changed since the last frame (<code>src/2_graph/10_renderer.ts:8</code>), so an adapter can update in place instead of re-rendering the world.</p><table tabindex="0"><thead><tr><th>field</th><th>content</th></tr></thead><tbody><tr><td><code>enterIds</code></td><td>ids in the new frame that were absent before</td></tr><tr><td><code>updateIds</code></td><td>ids present in both frames</td></tr><tr><td><code>exitIds</code></td><td>ids in the previous frame now gone</td></tr></tbody></table><p><code>graphRenderReceipt</code> computes these from the sorted id sets (<code>src/2_graph/10_renderer.ts:25</code>). The caller keeps <code>previousIds</code> across frames and feeds it back on the next call, which is why the same resource moves a diagram forward frame by frame.</p><h2 id="document-renderer-gestures" tabindex="-1">Document renderer gestures <a class="header-anchor" href="#document-renderer-gestures" aria-label="Permalink to “Document renderer gestures”">​</a></h2><p>The document renderer answers a fixed set of gestures, named in <code>DOCUMENT_GESTURES</code> (<code>packages/grapht/src/2_graph/14_gestureLegend.ts:9</code>), in the order a reader tries them.</p><table tabindex="0"><thead><tr><th>gesture</th><th>action</th></tr></thead><tbody><tr><td>scroll</td><td>pan up and down</td></tr><tr><td>shift + scroll</td><td>pan left and right</td></tr><tr><td>cmd + scroll</td><td>zoom at the cursor</td></tr><tr><td>pinch</td><td>zoom at the cursor</td></tr><tr><td>drag</td><td>pan</td></tr><tr><td>drag on text</td><td>select text</td></tr></tbody></table><p><code>createGestureLegend(host, hints)</code> renders a corner legend for these (<code>src/2_graph/14_gestureLegend.ts:52</code>). It returns an element, a <code>setOpen</code> switch, a <code>toggled$</code> Observable that the application runs at its own boundary, and a <code>remove</code> handle.</p>`,18)]])}var s=n(a,[[`render`,o]]);export{i as __pageData,s as default};