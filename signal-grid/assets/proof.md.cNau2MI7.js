import{At as e,Q as t,St as n,at as r,it as i,nt as a,ot as o,st as s,t as c,vt as l}from"./chunks/framework.B8IeflXD.js";var u=JSON.parse(`{"title":"Recorded proof","description":"","frontmatter":{},"headers":[],"relativePath":"proof.md","filePath":"pages/4_proof.md"}`),d={name:`proof.md`};function f(c,u,d,f,p,m){let h=n(`Mermaid`);return l(),i(`div`,null,[u[1]||=r(`<h1 id="recorded-proof" tabindex="-1">Recorded proof <a class="header-anchor" href="#recorded-proof" aria-label="Permalink to “Recorded proof”">​</a></h1><p>Five scenarios, filmed in real chromium, each one asserted on measured geometry and real DOM before and after every step. The video is the evidence a person can watch; the assertions are the gate that fails the build when the evidence stops being true.</p><table tabindex="0"><thead><tr><th></th><th></th></tr></thead><tbody><tr><td>suite</td><td><code>tests/2_visual.e2e.test.ts</code></td></tr><tr><td>config</td><td><code>vitest.visual.config.ts</code> (video <code>on</code>, trace <code>on</code>, 1400x900, <code>contextScope: &quot;test&quot;</code>)</td></tr><tr><td>run</td><td><code>npx vitest run -c vitest.visual.config.ts</code> then <code>node scripts/videos.mjs</code></td></tr><tr><td>result</td><td>5 passed, 0 failed, 20.56 s</td></tr><tr><td>clips</td><td><code>out/visual/mp4/*.mp4</code>, index at <code>out/visual/index.md</code></td></tr></tbody></table><h2 id="toc" tabindex="-1">TOC <a class="header-anchor" href="#toc" aria-label="Permalink to “TOC”">​</a></h2><ol><li><a href="#_1-what-the-page-under-test-is">What the page under test is</a></li><li><a href="#_2-scenario-1-css-customization">Scenario 1: CSS customization</a></li><li><a href="#_3-scenario-2-column-resize-by-dragging">Scenario 2: column resize by dragging</a></li><li><a href="#_4-scenario-3-virtualization">Scenario 3: virtualization</a></li><li><a href="#_5-scenario-4-column-order-model">Scenario 4: column order model</a></li><li><a href="#_6-scenario-5-column-visibility">Scenario 5: column visibility</a></li><li><a href="#_7-clip-inventory">Clip inventory</a></li></ol><h2 id="_1-what-the-page-under-test-is" tabindex="-1">1. What the page under test is <a class="header-anchor" href="#_1-what-the-page-under-test-is" aria-label="Permalink to “1. What the page under test is”">​</a></h2>`,6),(l(),a(t,null,{default:e(()=>[s(h,{id:`mermaid-103`,class:`mermaid`,graph:`flowchart%20LR%0A%20%20A%5B%22src%2Findex.ts%22%5D%20--%3E%7Cvite%20lib%20build%2C%20iife%7C%20B%5B%22one%20script%2C%20658%20KB%22%5D%0A%20%20C%5B%22src%2Ftheme.css%22%5D%20--%3E%20D%0A%20%20B%20--%3E%7Cpage.addScriptTag%7C%20D%5B%22chromium%20page%22%5D%0A%20%20D%20--%3E%7C%22grid()%20%2B%20render()%22%7C%20E%5B%22real%20grid%20on%20window.__grid%22%5D%0A%20%20F%5B%22playwright%20mouse%2C%3Cbr%2F%3EemulateMedia%2C%3Cbr%2F%3Estate%20writes%22%5D%20--%3E%20E%0A%20%20E%20--%3E%20G%5B%22assertions%20on%3Cbr%2F%3EgetBoundingClientRect%2C%3Cbr%2F%3EgetComputedStyle%2C%3Cbr%2F%3Edata-*%20attributes%22%5D%0A%20%20D%20--%3E%7CrecordVideo%7C%20H%5B%22out%2Fvisual%2F**.webm%22%5D%0A%20%20H%20--%3E%7Cscripts%2Fvideos.mjs%7C%20I%5B%22out%2Fvisual%2Fmp4%2F*.mp4%22%5D%0A`})]),fallback:e(()=>[...u[0]||=[o(` Loading... `,-1)]]),_:1})),u[2]||=r(`<p><code>fixtures/main.ts</code> builds a fixed receipt page by calling <code>grid()</code> and <code>render()</code> (<code>fixtures/main.ts:107,126</code>). The visual suite builds its own page instead, from <code>src/index.ts</code> with a vite lib build in <code>beforeAll</code>, so it can inject <code>src/theme.css</code> unmodified and mount a live grid per test. Nothing between the assertions and the library is a test double.</p><p><code>render()</code> installs the scroll and resize listeners that feed the viewport (<code>src/10_render.ts:583-604</code>), so the viewport needs no consumer wiring. The resize drag in the injected bootstrap is assembled from parts the package already exports: the delegated <code>gridDom(id).headerResize.route.pointerdown</code> stream, <code>drag()</code> from <code>src/6_gestures.ts</code>, and one <code>{ phase: &quot;change&quot;, type: &quot;colWidth&quot; }</code> dispatch per move.</p><h2 id="_2-scenario-1-css-customization" tabindex="-1">2. Scenario 1: CSS customization <a class="header-anchor" href="#_2-scenario-1-css-customization" aria-label="Permalink to “2. Scenario 1: CSS customization”">​</a></h2><p><code>out/visual/mp4/tests-2-visual-e2e-test__css-custom-properties-on-the-grid-root-retheme-live-rows-and-light-dark-flips-wi.mp4</code> (5.64 s, 53 KB)</p><p>Proves the theme is data: a consumer retheme is a custom property write on the grid root, and the <code>light-dark()</code> pairs in <code>src/theme.css</code> re-resolve from the real color scheme with no re-render.</p><p>Grid: tree data, 3 folders x 4 leaves, <code>expanded: { p0: true }</code>, columns Name / Size / Kind.</p><table tabindex="0"><thead><tr><th>step</th><th>measured</th><th>assertion</th></tr></thead><tbody><tr><td>baseline</td><td>row 36 px, depth-1 indent 16 px</td><td><code>boxOf(row).height === 36</code>, <code>margin-inline-start === &quot;16px&quot;</code></td></tr><tr><td><code>--sg-row-h: 52px</code>, <code>--sg-indent: 40px</code></td><td>row 52 px, indent 40 px</td><td>same two reads, new values</td></tr><tr><td><code>--sg-selected-bg: rgb(255, 64, 129)</code> + <code>rowSelection = { p0: true }</code></td><td>selected row <code>rgb(255, 64, 129)</code>, sibling <code>rgb(255, 255, 255)</code></td><td><code>data-selected=&quot;true&quot;</code> and computed <code>background-color</code> on both rows</td></tr><tr><td><code>emulateMedia({ colorScheme: &quot;dark&quot; })</code></td><td>bg <code>rgb(20, 22, 26)</code>, fg <code>rgb(230, 232, 234)</code></td><td>grid root and an unselected row both flip</td></tr><tr><td><code>emulateMedia({ colorScheme: &quot;light&quot; })</code></td><td>bg <code>rgb(255, 255, 255)</code>, fg <code>rgb(26, 28, 31)</code></td><td>the same declarations resolve to the other pair</td></tr><tr><td><code>state.density = &quot;compact&quot;</code></td><td>row 28 px, <code>--sg-row-h</code> reads <code>28px</code></td><td>the renderer reclaims the property from the override</td></tr></tbody></table><p><code>--sg-row-h</code> has two writers: the default in <code>src/theme.css</code> and <code>writeGridVars</code> in <code>src/9_css.ts</code>, which writes it inline from <code>ROW_HEIGHT[density]</code> on every geometry frame. A run-time override on the grid root therefore holds until the next geometry frame, and the density step is what takes it back. The scenario asserts both halves so the ownership is recorded rather than discovered later.</p><h2 id="_3-scenario-2-column-resize-by-dragging" tabindex="-1">3. Scenario 2: column resize by dragging <a class="header-anchor" href="#_3-scenario-2-column-resize-by-dragging" aria-label="Permalink to “3. Scenario 2: column resize by dragging”">​</a></h2><p><code>out/visual/mp4/tests-2-visual-e2e-test__dragging-the-resize-handle-with-a-real-pointer-widens-one-column-and-leaves-its-.mp4</code> (2.96 s, 76 KB)</p><p>Proves a resize is one state write that both bands read, and that it touches exactly one column.</p><p>Grid: 40 flat rows, columns Name 220 / Size 120 / Kind 140 / Owner 160, all <code>resizable</code>.</p><p>Gesture path, not state path. <code>page.mouse.move</code> to the centre of <code>[data-route=&quot;h&quot;][data-col-id=&quot;size&quot;] [data-route=&quot;resize&quot;]</code>, <code>mouse.down</code>, six <code>mouse.move</code> calls of +20 px each with a dwell between, <code>mouse.up</code>.</p><table tabindex="0"><thead><tr><th>move</th><th>measured cell width</th></tr></thead><tbody><tr><td>+20</td><td>140 px</td></tr><tr><td>+40</td><td>160 px</td></tr><tr><td>+60</td><td>180 px</td></tr><tr><td>+80</td><td>200 px</td></tr><tr><td>+100</td><td>220 px</td></tr><tr><td>+120</td><td>240 px</td></tr></tbody></table><p>Assertions after release:</p><ul><li><code>boxOf(row r0, col size).width === 240</code></li><li><code>boxOf(header size).width === 240</code>, so one write moved the header band and the row band</li><li><code>__grid.widths().size === 240</code>, the model agrees with the document</li><li><code>getComputedStyle(root).getPropertyValue(&quot;--sg-h-w-size&quot;) === &quot;240px&quot;</code></li><li>neighbour Name: width 220 px and x unchanged across the whole drag</li><li>neighbour Kind: width 140 px unchanged (its offset moves, its size does not)</li></ul><h2 id="_4-scenario-3-virtualization" tabindex="-1">4. Scenario 3: virtualization <a class="header-anchor" href="#_4-scenario-3-virtualization" aria-label="Permalink to “4. Scenario 3: virtualization”">​</a></h2><p><code>out/visual/mp4/tests-2-visual-e2e-test__5000-rows-recycle-through-a-bounded-dom-and-turning-virtualization-off-renders-t.mp4</code> (9.32 s, 168 KB)</p><p>Proves rows recycle: the model holds 5000, the document holds around 25, and the two stay in agreement while the list scrolls.</p><p>Grid: 5000 flat rows, columns Name / Size / Kind, scroll box 620 px tall, <code>overscan</code> 4.</p><table tabindex="0"><thead><tr><th>step</th><th>input</th><th>DOM <code>[data-route=&quot;r&quot;]</code></th><th>first row key</th></tr></thead><tbody><tr><td>mounted</td><td>none</td><td>22</td><td><code>r0</code></td></tr><tr><td>wheel 1 of 3</td><td>6 x <code>mouse.wheel(0, 340)</code></td><td>26</td><td><code>r52</code></td></tr><tr><td>wheel 2 of 3</td><td>6 x <code>mouse.wheel(0, 340)</code></td><td>26</td><td><code>r109</code></td></tr><tr><td>wheel 3 of 3</td><td>6 x <code>mouse.wheel(0, 340)</code></td><td>26</td><td><code>r166</code></td></tr><tr><td>jump to the end</td><td><code>scrollTop = 1e6</code>, clamped</td><td>21</td><td><code>r4979</code></td></tr><tr><td><code>state.virtualize.vertical = false</code></td><td>none</td><td><strong>5000</strong></td><td><code>r0</code></td></tr><tr><td><code>state.virtualize.vertical = true</code></td><td>none</td><td>21</td><td><code>r4979</code></td></tr></tbody></table><p>Assertions: <code>__grid.flat() === 5000</code> throughout; every windowed count under 60; every first key distinct across the five scroll steps, which is the recycling claim; <code>plan.center.length</code> equal to the DOM count at each step, so the model and the document never disagree; the clamped end contains <code>r4999</code>; and <code>virtualize.vertical: false</code> puts all 5000 rows in the document and in <code>plan.center</code>.</p><p>Peak ratio: 5000 model rows to 26 DOM rows, 192 to 1.</p><h2 id="_5-scenario-4-column-order-model" tabindex="-1">5. Scenario 4: column order model <a class="header-anchor" href="#_5-scenario-4-column-order-model" aria-label="Permalink to “5. Scenario 4: column order model”">​</a></h2><p><code>out/visual/mp4/tests-2-visual-e2e-test__state-colorder-drives-the-header-band-and-every-row-band-from-one-model.mp4</code> (3.16 s, 64 KB)</p><p>Proves one array orders both bands. A header-only reorder would pass a header assertion and fail this one, because every one of the 12 rows is read.</p><p>Grid: 12 flat rows, columns Name / Size / Kind / Owner.</p><table tabindex="0"><thead><tr><th>write</th><th>header sequence</th><th>cell sequence, all 12 rows</th></tr></thead><tbody><tr><td>mount, <code>colOrder = [&quot;name&quot;,&quot;size&quot;,&quot;kind&quot;,&quot;owner&quot;]</code></td><td>name, size, kind, owner</td><td>identical</td></tr><tr><td><code>colOrder = [&quot;kind&quot;,&quot;owner&quot;,&quot;name&quot;,&quot;size&quot;]</code></td><td>kind, owner, name, size</td><td>identical</td></tr><tr><td><code>colOrder = [&quot;owner&quot;,&quot;name&quot;,&quot;size&quot;,&quot;kind&quot;]</code></td><td>owner, name, size, kind</td><td>identical</td></tr></tbody></table><p>The third write moves the previous first column (<code>kind</code>) to last. <code>__grid.order()</code>, read straight off <code>g.view.cols</code>, matches the document in the same tick.</p><h2 id="_6-scenario-5-column-visibility" tabindex="-1">6. Scenario 5: column visibility <a class="header-anchor" href="#_6-scenario-5-column-visibility" aria-label="Permalink to “6. Scenario 5: column visibility”">​</a></h2><p><code>out/visual/mp4/tests-2-visual-e2e-test__hiding-a-column-removes-it-from-both-bands-and-reflows-the-rest-and-unhiding-ret.mp4</code> (3.12 s, 61 KB)</p><p>Proves hiding is a model operation, not a <code>display: none</code>: the cells leave the document, the remaining flex columns take the freed width, and unhiding restores rank rather than appending.</p><p>Grid: 12 flat rows, columns Name <code>flex 2 minWidth 120</code>, Size / Kind / Owner <code>flex 1 minWidth 80</code>, <code>colOrder = [&quot;name&quot;,&quot;size&quot;,&quot;kind&quot;,&quot;owner&quot;]</code>, available width 1344 px.</p><table tabindex="0"><thead><tr><th>state</th><th>header sequence</th><th><code>[data-col-id=&quot;kind&quot;]</code> cells</th><th>Name px</th><th>Size px</th></tr></thead><tbody><tr><td>all visible</td><td>name, size, kind, owner</td><td>12</td><td>536.80</td><td>268.39</td></tr><tr><td><code>colHidden = { kind: true }</code></td><td>name, size, owner</td><td>0</td><td>671.00</td><td>335.50</td></tr><tr><td><code>colHidden = {}</code></td><td>name, size, kind, owner</td><td>12</td><td>536.80</td><td>268.40</td></tr></tbody></table><p>Assertions: <code>toHaveCount(0)</code> for both the hidden header cell and its row cells; every row&#39;s cell sequence equal to <code>[&quot;name&quot;,&quot;size&quot;,&quot;owner&quot;]</code> while hidden; both remaining widths strictly greater than before; <code>round(name / size) === 2</code>, so the 2:1:1 flex ratio survived the reflow; and on unhide, <code>headerOrder().indexOf(&quot;kind&quot;) === 2</code>, its rank in <code>colOrder</code>, not the end.</p><h2 id="_7-clip-inventory" tabindex="-1">7. Clip inventory <a class="header-anchor" href="#_7-clip-inventory" aria-label="Permalink to “7. Clip inventory”">​</a></h2><table tabindex="0"><thead><tr><th>scenario</th><th>mp4</th><th>seconds</th><th>KB</th></tr></thead><tbody><tr><td>1 CSS customization</td><td><code>…__css-custom-properties-on-the-grid-root-retheme-live-rows-and-light-dark-flips-wi.mp4</code></td><td>5.64</td><td>53</td></tr><tr><td>2 column resize</td><td><code>…__dragging-the-resize-handle-with-a-real-pointer-widens-one-column-and-leaves-its-.mp4</code></td><td>2.96</td><td>76</td></tr><tr><td>3 virtualization</td><td><code>…__5000-rows-recycle-through-a-bounded-dom-and-turning-virtualization-off-renders-t.mp4</code></td><td>9.32</td><td>168</td></tr><tr><td>4 column order</td><td><code>…__state-colorder-drives-the-header-band-and-every-row-band-from-one-model.mp4</code></td><td>3.16</td><td>64</td></tr><tr><td>5 column visibility</td><td><code>…__hiding-a-column-removes-it-from-both-bands-and-reflows-the-rest-and-unhiding-ret.mp4</code></td><td>3.12</td><td>61</td></tr></tbody></table><p>Byte sizes come from the run of 2026-09-10 08:56 and move a few KB between runs, because vp8 encoding is not bit-identical; <code>scripts/videos.mjs</code> rewrites <code>out/visual/index.md</code> with the current numbers every time it runs. Durations are stable.</p><p>All five sit under <code>out/visual/mp4/</code>, prefixed <code>tests-2-visual-e2e-test__</code>. The source <code>.webm</code>, the playwright trace, and a failure screenshot when there is one stay in the attempt directory beside them, under <code>out/visual/tests-2-visual-e2e-test/&lt;test-slug&gt;.&lt;task id&gt;.r&lt;retry&gt;.p&lt;repeat&gt;/</code>.</p><p>Each clip narrates itself: <code>tests/helpers/record.ts</code> paints a caption band naming the action about to happen and a drawn cursor tracking the real pointer, both outside the grid root and both <code>pointer-events: none</code>, so playwright&#39;s actionability hit test still lands on the cell underneath and no assertion can see either element.</p>`,40)])}var p=c(d,[[`render`,f]]);export{u as __pageData,p as default};