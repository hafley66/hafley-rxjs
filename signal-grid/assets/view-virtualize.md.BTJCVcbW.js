import{St as e,at as t,it as n,ot as r,st as i,t as a,tt as o,vt as s}from"./chunks/framework.DkAysg6d.js";var c=JSON.parse(`{"title":"Virtualization","description":"","frontmatter":{},"headers":[],"relativePath":"view-virtualize.md","filePath":"pages/view-virtualize.md"}`),l={name:`view-virtualize.md`};function u(a,c,l,u,d,f){let p=e(`GridDemo`);return s(),n(`div`,null,[c[0]||=o(`h1`,{id:`virtualization`,tabindex:`-1`},[r(`Virtualization `),o(`a`,{class:`header-anchor`,href:`#virtualization`,"aria-label":`Permalink to “Virtualization”`},`​`)],-1),c[1]||=o(`p`,null,`Render only the rows the viewport can show, and turn that off with one write when you want the whole list in the document.`,-1),i(p,{id:`virtualization-50k`}),c[2]||=t(`<h2 id="the-switch" tabindex="-1">The switch <a class="header-anchor" href="#the-switch" aria-label="Permalink to “The switch”">​</a></h2><p><code>virtualize</code> carries one flag per seat, keyed the way <code>CellSpan</code> is keyed. <code>vertical</code> gates the run that scrolls and pages, <code>horizontal</code> gates the run across the page, and <code>orientation</code> decides which axis is sitting in each seat.</p><div class="language-ts"><button title="Copy code" data-copied="Copied" class="copy"></button><span class="lang">ts</span><pre class="shiki shiki-themes github-light github-dark" style="--shiki-light:#24292e;--shiki-dark:#e1e4e8;--shiki-light-bg:#fff;--shiki-dark-bg:#24292e;" tabindex="0" dir="ltr"><code><span class="line"><span style="--shiki-light:#24292E;--shiki-dark:#E1E4E8;">g.state.virtualize.vertical.</span><span style="--shiki-light:#6F42C1;--shiki-dark:#B392F0;">$</span><span style="--shiki-light:#24292E;--shiki-dark:#E1E4E8;">(</span><span style="--shiki-light:#005CC5;--shiki-dark:#79B8FF;">false</span><span style="--shiki-light:#24292E;--shiki-dark:#E1E4E8;">)</span></span>
<span class="line"><span style="--shiki-light:#24292E;--shiki-dark:#E1E4E8;">g.view.plan.</span><span style="--shiki-light:#6F42C1;--shiki-dark:#B392F0;">$</span><span style="--shiki-light:#24292E;--shiki-dark:#E1E4E8;">().center     </span><span style="--shiki-light:#62687b;--shiki-dark:#818e99;">// every row in the page</span></span></code></pre></div><p>Off makes the window step the identity. Every other stage runs exactly as before, which is the property that makes the toggle safe to flip in a test: the plan changes size and nothing changes shape.</p><h2 id="what-the-window-needs" tabindex="-1">What the window needs <a class="header-anchor" href="#what-the-window-needs" aria-label="Permalink to “What the window needs”">​</a></h2><table tabindex="0"><thead><tr><th>input</th><th>source</th></tr></thead><tbody><tr><td>the viewport box</td><td><code>config.viewport</code>, a source of top, left, width, and height</td></tr><tr><td>the sizer</td><td><code>sizerFor</code> in <code>src/8_grid.ts</code>, built over the page run</td></tr><tr><td>the padding</td><td><code>config.overscan</code>, rows added on each side of the visible slice</td></tr></tbody></table><p><code>windowOf</code> in <code>src/4_slice.ts</code> turns those into an index range, and the kernel below it is a pure function of that range. Overscan lives in the viewport-to-range step for that reason, so no stage downstream has to know the window was padded.</p><h2 id="the-viewport-is-yours-to-feed" tabindex="-1">The viewport is yours to feed <a class="header-anchor" href="#the-viewport-is-yours-to-feed" aria-label="Permalink to “The viewport is yours to feed”">​</a></h2><p><code>grid()</code> subscribes to no producer for the viewport, so a scroll listener or a resize observer writing that source is consumer glue. <code>render</code> wires the common case for you.</p><h2 id="why-the-sizer-is-built-over-the-page-run" tabindex="-1">Why the sizer is built over the page run <a class="header-anchor" href="#why-the-sizer-is-built-over-the-page-run" aria-label="Permalink to “Why the sizer is built over the page run”">​</a></h2><p>Pinning lifts rows out of the middle and paging drops others, so an index into the flat list names a different row than the same index into the run being windowed. Building the sizer over the page run keeps the arithmetic honest at the only place the pixels are used.</p><h2 id="the-other-seat" tabindex="-1">The other seat <a class="header-anchor" href="#the-other-seat" aria-label="Permalink to “The other seat”">​</a></h2><p><code>g.state.virtualize.horizontal.$(true)</code> runs the same <code>renderPlan</code> over the horizontal run, which <code>src/8_grid.ts:542</code> builds as <code>colPlan</code>. It is off by default, because a schema narrow enough to fit pays the two spacer tracks for nothing.</p><p>Under <code>orientation: &quot;columns&quot;</code> the vertical seat holds columns and the horizontal seat holds rows, so neither flag is named after an axis: the seat is what each one gates.</p><h2 id="measured-content" tabindex="-1">Measured content <a class="header-anchor" href="#measured-content" aria-label="Permalink to “Measured content”">​</a></h2><p><code>src/14_measure.ts</code> carries one resize observer per store, an estimate for unmeasured rows, a buffer zone, and scroll anchoring, which is the path for rows whose height you cannot declare. Declared heights are on <a href="/hafley-rxjs/signal-grid/rows-height">Height</a>.</p>`,16)])}var d=a(l,[[`render`,u]]);export{c as __pageData,d as default};