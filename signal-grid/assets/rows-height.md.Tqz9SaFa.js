import{St as e,at as t,it as n,ot as r,st as i,t as a,tt as o,vt as s}from"./chunks/framework.B8IeflXD.js";var c=JSON.parse(`{"title":"Height","description":"","frontmatter":{},"headers":[],"relativePath":"rows-height.md","filePath":"pages/rows-height.md"}`),l={name:`rows-height.md`};function u(a,c,l,u,d,f){let p=e(`GridDemo`);return s(),n(`div`,null,[c[0]||=o(`h1`,{id:`height`,tabindex:`-1`},[r(`Height `),o(`a`,{class:`header-anchor`,href:`#height`,"aria-label":`Permalink to “Height”`},`​`)],-1),c[1]||=o(`p`,null,`Give one row its own height while the rest fall through to the density default, and keep scrolling accurate while you do it.`,-1),i(p,{id:`row-height`}),c[2]||=t(`<h2 id="the-state" tabindex="-1">The state <a class="header-anchor" href="#the-state" aria-label="Permalink to “The state”">​</a></h2><div class="language-ts"><button title="Copy code" data-copied="Copied" class="copy"></button><span class="lang">ts</span><pre class="shiki shiki-themes github-light github-dark" style="--shiki-light:#24292e;--shiki-dark:#e1e4e8;--shiki-light-bg:#fff;--shiki-dark-bg:#24292e;" tabindex="0" dir="ltr"><code><span class="line"><span style="--shiki-light:#24292E;--shiki-dark:#E1E4E8;">g.state.rowHeight.r3.</span><span style="--shiki-light:#6F42C1;--shiki-dark:#B392F0;">$</span><span style="--shiki-light:#24292E;--shiki-dark:#E1E4E8;">(</span><span style="--shiki-light:#005CC5;--shiki-dark:#79B8FF;">72</span><span style="--shiki-light:#24292E;--shiki-dark:#E1E4E8;">)</span></span>
<span class="line"><span style="--shiki-light:#24292E;--shiki-dark:#E1E4E8;">g.state.rowHeight.</span><span style="--shiki-light:#6F42C1;--shiki-dark:#B392F0;">$</span><span style="--shiki-light:#24292E;--shiki-dark:#E1E4E8;">({ r3: </span><span style="--shiki-light:#005CC5;--shiki-dark:#79B8FF;">72</span><span style="--shiki-light:#24292E;--shiki-dark:#E1E4E8;">, r9: </span><span style="--shiki-light:#005CC5;--shiki-dark:#79B8FF;">120</span><span style="--shiki-light:#24292E;--shiki-dark:#E1E4E8;"> })</span></span>
<span class="line"><span style="--shiki-light:#24292E;--shiki-dark:#E1E4E8;">g.state.rowHeight.r3.</span><span style="--shiki-light:#6F42C1;--shiki-dark:#B392F0;">$</span><span style="--shiki-light:#24292E;--shiki-dark:#E1E4E8;">(</span><span style="--shiki-light:#005CC5;--shiki-dark:#79B8FF;">undefined</span><span style="--shiki-light:#24292E;--shiki-dark:#E1E4E8;">)   </span><span style="--shiki-light:#62687b;--shiki-dark:#818e99;">// back to the default</span></span></code></pre></div><p>The default comes from density, covered on <a href="/hafley-rxjs/signal-grid/view-density">Density</a>.</p><h2 id="what-an-override-costs" tabindex="-1">What an override costs <a class="header-anchor" href="#what-an-override-costs" aria-label="Permalink to “What an override costs”">​</a></h2><p>The window has to know where every row starts, and a uniform height makes that arithmetic. One override makes it a lookup.</p><table tabindex="0"><thead><tr><th>sizer</th><th>cost per lookup</th><th>chosen when</th></tr></thead><tbody><tr><td><code>uniformSizer</code> in <code>src/4_slice.ts</code></td><td>constant</td><td>no key in the run carries an override</td></tr><tr><td><code>measuredSizer</code> in <code>src/4_slice.ts</code></td><td>a prefix sum with a binary search</td><td>any key in the run carries one</td></tr></tbody></table><p><code>sizerFor</code> in <code>src/8_grid.ts</code> picks between them, and it looks at the page run rather than the whole list, so an override on a row you paged away costs nothing.</p><h2 id="the-pixels" tabindex="-1">The pixels <a class="header-anchor" href="#the-pixels" aria-label="Permalink to “The pixels”">​</a></h2><p>A height reaches the DOM as a custom property named after the row, encoded to a valid identifier by <code>encodeVarId</code> in <code>src/3_paths.ts</code>. The generic rule cannot spell that name, so the element also carries a short alias the stylesheet reads.</p><div class="language-"><button title="Copy code" data-copied="Copied" class="copy"></button><span class="lang"></span><pre class="shiki shiki-themes github-light github-dark" style="--shiki-light:#24292e;--shiki-dark:#e1e4e8;--shiki-light-bg:#fff;--shiki-dark-bg:#24292e;" tabindex="0" dir="ltr"><code><span class="line"><span>--sg-r-h-&lt;encoded row id&gt;   the value, written by the property writer</span></span>
<span class="line"><span>--sg-h                      the per-row alias a rule can match</span></span></code></pre></div><p><code>writeGridVars</code> in <code>src/9_css.ts</code> writes one pass per frame and drops a property whose row left the plan, so the sheet never grows without bound while you scroll.</p><h2 id="detail-panels" tabindex="-1">Detail panels <a class="header-anchor" href="#detail-panels" aria-label="Permalink to “Detail panels”">​</a></h2><p>A detail panel is a row, so its height is a <code>rowHeight</code> entry like any other. <code>detailHeights</code> in <code>src/11_detail.ts</code> writes those entries for you. See <a href="/hafley-rxjs/signal-grid/rows-detail">Detail rows</a>.</p><h2 id="measuring-instead-of-declaring" tabindex="-1">Measuring instead of declaring <a class="header-anchor" href="#measuring-instead-of-declaring" aria-label="Permalink to “Measuring instead of declaring”">​</a></h2><p><code>src/14_measure.ts</code> holds one resize observer per store, an estimate, a buffer zone, and scroll anchoring, which is the path for content whose height you cannot know in advance.</p>`,15)])}var d=a(l,[[`render`,u]]);export{c as __pageData,d as default};