import{at as e,it as t,t as n,vt as r}from"./chunks/framework.DkAysg6d.js";var i=JSON.parse(`{"title":"Two ordered forests","description":"","frontmatter":{},"headers":[],"relativePath":"why-forests.md","filePath":"pages/why-forests.md"}`),a={name:`why-forests.md`};function o(n,i,a,o,s,c){return r(),t(`div`,null,[...i[0]||=[e(`<h1 id="two-ordered-forests" tabindex="-1">Two ordered forests <a class="header-anchor" href="#two-ordered-forests" aria-label="Permalink to “Two ordered forests”">​</a></h1><p>Rows and columns are the same container, so every structural operator is written once and called twice.</p><div class="language-ts"><button title="Copy code" data-copied="Copied" class="copy"></button><span class="lang">ts</span><pre class="shiki shiki-themes github-light github-dark" style="--shiki-light:#24292e;--shiki-dark:#e1e4e8;--shiki-light-bg:#fff;--shiki-dark-bg:#24292e;" tabindex="0" dir="ltr"><code><span class="line"><span style="--shiki-light:#24292E;--shiki-dark:#E1E4E8;">Axis</span><span style="--shiki-light:#c62739;--shiki-dark:#F97583;">&lt;</span><span style="--shiki-light:#24292E;--shiki-dark:#E1E4E8;">RowId, TRow</span><span style="--shiki-light:#c62739;--shiki-dark:#F97583;">&gt;</span><span style="--shiki-light:#62687b;--shiki-dark:#818e99;">          // the rows</span></span>
<span class="line"><span style="--shiki-light:#24292E;--shiki-dark:#E1E4E8;">Axis</span><span style="--shiki-light:#c62739;--shiki-dark:#F97583;">&lt;</span><span style="--shiki-light:#24292E;--shiki-dark:#E1E4E8;">ColId, ColumnDef</span><span style="--shiki-light:#c62739;--shiki-dark:#F97583;">&gt;</span><span style="--shiki-light:#62687b;--shiki-dark:#818e99;">     // the columns</span></span></code></pre></div><p><code>Axis&lt;K, T&gt;</code> carries <code>roots</code>, <code>children</code>, <code>parent</code>, and <code>by</code>, and is declared in <code>src/0_types.ts</code>. A flat grid is the case where every key is a root, and a column header group is a parent edge.</p><h2 id="the-usual-shape-beside-this-one" tabindex="-1">The usual shape, beside this one <a class="header-anchor" href="#the-usual-shape-beside-this-one" aria-label="Permalink to “The usual shape, beside this one”">​</a></h2><table tabindex="0"><thead><tr><th></th><th>a typical grid</th><th>this package</th></tr></thead><tbody><tr><td>rows</td><td>a pipeline of row models</td><td>one ordered forest</td></tr><tr><td>columns</td><td>an array plus a separate grouping model</td><td>the same ordered forest</td></tr><tr><td>the operators</td><td>one set per axis</td><td>five pure ones in <code>src/1_axis.ts</code>, shared</td></tr><tr><td>which axis scrolls</td><td>fixed when the library was written</td><td><code>state.orientation</code>, read through a lookup table</td></tr></tbody></table><h2 id="what-one-container-buys" tabindex="-1">What one container buys <a class="header-anchor" href="#what-one-container-buys" aria-label="Permalink to “What one container buys”">​</a></h2><table tabindex="0"><thead><tr><th>claim</th><th>receipt</th></tr></thead><tbody><tr><td>One flatten serves tree rows and header groups. <code>flattenAxis</code> is called with the real expansion predicate for rows and with a predicate that always answers open for columns.</td><td>two call sites in <code>src/8_grid.ts</code></td></tr><tr><td>One partition serves row pinning and column pinning, with <code>start</code>, <code>center</code>, <code>end</code> on both.</td><td><code>partition</code> in <code>src/4_slice.ts</code></td></tr><tr><td>A flat list and a tree run one path, because a flat grid is a forest whose every key is a root.</td><td><code>axisOfEntries</code> and <code>axisOfTree</code> in <code>src/1_axis.ts</code></td></tr><tr><td>A cyclic parent map does not throw. A node standing on a cycle becomes a root.</td><td><code>axisOfEntries</code> in <code>src/1_axis.ts</code>, with cycles among the cases in <code>src/1_axis.test.ts</code></td></tr><tr><td>Identity by reference is the skip condition, so a downstream stage compares and does nothing.</td><td>every operator in <code>src/1_axis.ts</code> returns its input when it changed nothing</td></tr></tbody></table><h2 id="measured" tabindex="-1">Measured <a class="header-anchor" href="#measured" aria-label="Permalink to “Measured”">​</a></h2><table tabindex="0"><thead><tr><th>what</th><th>this package</th><th>the comparison</th></tr></thead><tbody><tr><td>a no-write read of the derived list</td><td>0.000137 ms</td><td>0.000454 ms</td></tr><tr><td>expanding a large tree at one tenth open</td><td>0.214 ms</td><td>0.651 ms</td></tr></tbody></table><p>Both rows are recorded with their method and their machine in <code>bench/README.md</code>.</p><h2 id="what-it-costs" tabindex="-1">What it costs <a class="header-anchor" href="#what-it-costs" aria-label="Permalink to “What it costs”">​</a></h2><table tabindex="0"><thead><tr><th>cost</th><th>receipt</th></tr></thead><tbody><tr><td>No per-row and per-column handle objects. A key-and-map container answers with map lookups where the alternatives hand back a row object carrying its own methods.</td><td><code>docs/3_competitors.md</code></td></tr><tr><td>Grouping is slower, because <code>groupAxis</code> rebuilds the forest above the data edges so a unit travels with its own subtree. At a hundred thousand rows that is 45.0 ms against 32.9 ms.</td><td><code>bench/README.md</code></td></tr><tr><td><code>axisOfEntries</code> is quadratic on a path-shaped relation, which a flat relation and <code>axisOfTree</code> never reach.</td><td><code>bench/README.md</code></td></tr><tr><td>Two vocabularies meet at one file, and a span declared in rows and columns has to be crossed into neutral counts.</td><td><code>neutralSpan</code> in <code>src/12_transpose.ts</code></td></tr><tr><td><code>renderPlan</code> is linear in the row count per scroll tick, virtualized or not.</td><td><code>bench/README.md</code></td></tr></tbody></table><h2 id="the-payoff" tabindex="-1">The payoff <a class="header-anchor" href="#the-payoff" aria-label="Permalink to “The payoff”">​</a></h2><p>Because both axes are one container, transposing the grid is a state write rather than a second rendering path. That argument is on <a href="/hafley-rxjs/signal-grid/why-seats">The seat table</a>.</p>`,15)]])}var s=n(a,[[`render`,o]]);export{i as __pageData,s as default};