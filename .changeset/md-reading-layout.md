---
"@hafley66/md": patch
---

Size Markdown tables to header plus rows under a 70vh cap with a resize handle bounded by the content, fold each list from one twisty at its top, space sections, blocks, and list items further apart, rule level-1 headers in the theme hairline colour, indent section content inside its header with per-level colour guides, and run tables, code, and diagrams to the content column's right edge.
