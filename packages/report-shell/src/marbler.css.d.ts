// A bundler resolves this specifier to the stylesheet itself. Node has no rule that accepts a `.css`
// specifier, so the published `types` condition points here and keeps the subpath resolvable.
export {}
