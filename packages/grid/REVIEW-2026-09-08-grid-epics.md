| Receipt command | Exit code | Counts |
| --- | --- | --- |
| `pnpm --filter @hafley66/signals build && pnpm --filter @hafley66/signals exec vitest run` | 1 overall; build 0; Vitest 1 | 13 test files: 2 passed, 11 failed during loading; 8 tests passed; build emitted unresolved-dependency/type diagnostics despite exiting 0. |
| `pnpm --filter @hafley66/grid exec tsc --noEmit && pnpm --filter @hafley66/grid exec vitest run && pnpm --filter @hafley66/grid exec vitest run --config vitest.browser.config.ts && pnpm --filter @hafley66/grid build` | 1 at `tsc` | 0 tests executed; unit tests, browser tests, and build were not reached; missing React, RxJS, TanStack, and workspace package declarations. |
| `pnpm --filter @hafley66/report-shell receipts` | 1 at typecheck | 0 tests executed; tests and build were not reached; missing React and workspace package declarations. |
| `pnpm --filter @hafley66/boop-adapters receipts` | 1 at typecheck | 0 tests executed; subsequent steps were not reached; TS2688 missing `node` type definitions. |
| `pnpm --filter @hafley66/vitest-telemetry receipts` | 1 at typecheck | 0 tests executed; subsequent steps were not reached; TS2688 missing `node` type definitions. |

| Severity | File:line | Claim | Fix |
| --- | --- | --- | --- |
| defect | `packages/grid/src/12_treeTable.tsx:67` | Every mounted `TreeTable` constructs its own `runEpics` observable, so two tables sharing one grid dispatch two select/pivot effects per click, and replacing the columns array restarts pending column work. | Construct one shared column/default epic runtime per grid outside render and let mounts subscribe to that same observable. |
| defect | `packages/grid/src/2_createGrid.ts:110`; `packages/grid/src/10_treeColumn.ts:51` | `createGrid` registers only `config.epics`, so `columnDefs: treeColumnDefs(columns)` retains receiver metadata but never runs those receivers through `grid.epics$` or plain `useGrid`. | Collect column receivers from the authoritative column definitions when constructing the grid and share their lifetime with grid epics. |
| defect | `packages/signals/src/10_slice.ts:64` | `merge` subscribes epic outputs sequentially, so a synchronous startup action from the first epic is dispatched before later epics subscribe and is lost to those receivers. | Use an RxJS scheduling arrangement that attaches every receiver before draining startup emissions, with a regression using a synchronous startup epic and a dependent epic. |
| defect | `packages/signals/src/10_slice.ts:47`; `packages/signals/src/10_slice.ts:65` | Synchronous feedback re-enters `bus.next`, allowing later observers to receive an effect before its originating intent and making a long finite epic chain consume recursive stack frames. | Serialize dispatch and epic feedback through RxJS `queueScheduler` and verify causal order with observers attached both before and after the epics. |
| defect | `packages/grid/src/1_types.ts:57`; `packages/grid/src/2_createGrid.ts:72` | `GridChange` accepts `{ phase: "change", type: "sorting" }` without `sorting`, which writes `undefined` into required state and breaks readers such as `sort.length`. | Make changes a mapped discriminated union requiring the selected key's value, and preserve that correlation in the reducer. |
| defect | `packages/signals/src/10_slice.ts:28`; `packages/signals/src/10_slice.ts:50` | `ctx` remains optional for a required context type, and `as Ctx` lets an epic expecting `{ by: number }` receive `undefined`. | Require `ctx` when the context type excludes `undefined`, or accept a typed context factory. |
| defect | `packages/grid/src/2_createGrid.ts:92`; `packages/grid/src/3_react.ts:55` | Client rows still undergo lodash root sorting before TanStack recursive sorting, duplicating sorting and using property IDs instead of column accessors for the first pass. | Pass the consumer's rows through and use the registered `createSortedRowModel` as the sole client sorter. |
| defect | `packages/grid/src/12_treeTable.tsx:101`; `packages/grid/src/12_treeTable.tsx:108`; `packages/grid/src/11_treeTableRow.tsx:58` | Headers follow TanStack column order while body cells follow the original `columns` array, so changing `grid.state.columnOrder` puts cells under the wrong headings and widths. | Render body cells in TanStack's visible-cell order and retrieve each renderer from its column metadata, as `4_grid.tsx:95` already does. |
| defect | `packages/grid/src/11_treeTableRow.tsx:15` | `closest("td[data-column]")` can select a nested table's cell and combine its column ID with the outer row's data; bubbling from a nested grid can also dispatch through both rows. | Reject events whose nearest owning row is not `e.currentTarget` and resolve only cells directly owned by that row. |
| defect | `packages/grid/src/11_treeTableRow.tsx:68` | An expandable `toggleExpand` cell stops every click before intent dispatch, so an attached `pivotOnAltClick` receiver never sees Alt-click and the row expands instead. | Route cell gestures through intent dispatch and restrict the default expansion behavior to its intended unmodified gesture. |
| defect | `packages/grid/src/14_treeTableHead.tsx:58` | Header sorting executes unconditionally after intent dispatch, so a column epic cannot suppress it and an epic that changes sorting can be followed by another toggle. | Have the default header receiver invoke TanStack sorting through a defined handling policy that permits a column receiver to replace the default. |
| defect | `packages/boop-adapters/src/report-app/model.ts:76`; `packages/grid/src/8_pivot.ts:13`; `packages/report-shell/src/components/PivotStack.tsx:15` | Boop pivots retain hierarchical roots but filter only those roots, so Alt-clicking a matching child under a nonmatching parent can produce an empty pivot and omit the clicked session. | Supply all pivotable sessions as flat rows, or explicitly filter and expand a recursive pivot row model that includes matching descendants. |
| defect | `packages/grid/src/8_pivot.ts:8`; `packages/grid/src/10_treeColumn.ts:76` | `pivotOnAltClick` accepts any value projection while `pivotGrid` compares `row[columnId]`, so computed or deep-path columns can emit a pivot value the grid cannot match. | Carry a pivot accessor with the column definition and use the same accessor for both effect emission and filtering. |
| defect | `packages/grid/src/6_tree.tsx:83`; `packages/grid/src/11_treeTableRow.tsx:70` | The `GridTree` migration makes its only column `toggleExpand`, so clicking an expandable node no longer reaches the existing `onRowClick` selection callback. | Preserve label-click selection and keep expansion on the twisty, or emit both explicitly under the documented gesture policy. |
| defect | `packages/grid/src/6_tree.tsx:126`; `packages/grid/src/11_treeTableRow.tsx:52` | `GridTree` hover and selected rules assign `background`, but the new row has an inline `background`, so those rules no longer display the selected or hovered color. | Set `--grid-row-bg` in those rules, matching the renderer's supported styling contract. |
| defect | `packages/signals/src/6_Storage.ts:95`; `packages/signals/src/6_Storage.ts:119` | The new hash/history adapters install global event listeners without returning teardown, so even the seed subscription leaves listeners behind after `unsubscribe`. | Use RxJS `fromEvent` composition with an initial read, or return a repository-owned `unsubscribe` that unregisters every installed listener. |
| defect | `packages/signals/src/6_Storage.ts:36`; `packages/signals/src/6_Storage.ts:49`; `packages/signals/src/6_Storage.ts:68` | Existing storage binding subscriptions and localStorage/URL listeners have no teardown owner, and the new grid sync and consumer models inherit those retained bindings. | Give the storage binding an RxJS-owned lifetime and ensure the adapter listeners unsubscribe when its owner releases it. |
| defect | `packages/boop-adapters/src/report-app/model.ts:88`; `packages/vitest-telemetry/src/report-app/model.ts:134` | Both model factories discard their Marbler synchronization subscriptions, leaving updates active after the consumer is unmounted or a model is replaced. | Return a shared observable for the synchronization effect and subscribe through a mounted owner, or aggregate subscriptions behind `unsubscribe`. |
| defect | `packages/report-shell/src/layout.ts:69`; `packages/boop-adapters/src/report-app/components/App.tsx:72`; `packages/vitest-telemetry/src/report-app/components/App.tsx:44` | `layout` creates permanent subscriptions and writes DOM/storage while called from `useMemo`, so discarded renders and Strict Mode recalculation can leave duplicate bindings. | Separate layout state creation from its DOM effect and connect that effect through an RxJS subscription returned to React's effect teardown. |
| defect | `packages/signals/src/1_SignalCreator.ts:385`; `packages/signals/src/1_SignalCreator.ts:390` | The existing computed-signal dependency implements a manual subscriber count and permanently pins dependencies after a synchronous read, so read grid row memos can retain subscriptions after their last observer unsubscribes. | Move connection counting to RxJS sharing and define synchronous reads without permanently pinning the dependency subscriptions. |
| defect | `packages/grid/src/2_createGrid.ts:38`; `packages/grid/src/12_treeTable.tsx:81` | A default page size of `100_000` still silently truncates a larger root set in `TreeTable`, which renders no pagination control to reach the remaining roots. | Disable pagination for the unpaginated tree view through TanStack's supported row-model options and enable it only when requested. |
| defect | `packages/grid/src/10_treeColumn.ts:87`; `packages/grid/src/13_columnVisibilityToolbar.tsx:48` | The visibility helpers duplicate TanStack visibility and hideability behavior while the renderer independently filters the same state. | Use TanStack column visibility APIs and `enableHiding` for the tree-column constraint, sharing the authoritative table model. |
| smell | `packages/grid/src/10_treeColumn.ts:71`; `packages/grid/src/10_treeColumn.ts:81`; `packages/grid/src/2_createGrid.ts:137` | Broad `as GridAction<TData>` assertions bypass checking of emitted action shapes, including the computed-key change action. | Give map callbacks concrete effect return types or use `satisfies`, and use a correlated key/value change constructor with any necessary computed-key assertion confined to that boundary. |
| smell | `packages/grid/src/10_treeColumn.ts:69`; `packages/grid/src/10_treeColumn.ts:79`; `packages/grid/src/12_treeTable.tsx:76`; `packages/grid/src/16_epics.test.ts:64`; `packages/grid/src/16_epics.test.ts:87` | Explicit `Extract<typeof a, ...>` guards are checked annotations rather than runtime holes, but the casts after compound filters and broad action assertions are avoidable. | Use inferred single-discriminant filters followed by modifier filters and explicitly typed action-producing callbacks; use the existing effect hook for the select subscription. |
| smell | `packages/boop-adapters/src/report-app/components/App.tsx:79`; `packages/vitest-telemetry/src/report-app/components/Nav.tsx:64`; `packages/grid/src/1_types.ts:61` | Both handlers erase the effect to `{ value: unknown }`, stringify arbitrary values, and hard-code `status`, although `useGridEffect` already supplies a typed row and column. | Preserve the inferred effect and use its column plus a validated value or typed `row.status`; retain `unknown` only at the arbitrary generic payload boundary, or parameterize effects by a column/value map. |
| smell | `packages/grid/src/10_treeColumn.ts:46`; `packages/grid/src/10_treeColumn.ts:71`; `packages/grid/README.md:283` | A stranger supplying only `Grid` and `TreeColumn[]` gets a select effect without selection state/highlighting, sorting only with `sortValue`, and no pivot unless an epic and consumer handler are supplied. | Provide a documented preset/example wiring selection, sortable accessors, `pivotOnAltClick`, and pivot presentation, and expose the required choices in the public API. |
| smell | `packages/signals/src/10_slice.ts:21`; `packages/grid/src/3_react.ts:29`; `packages/grid/README.md:154` | Epic lifetime now depends on explicit `epics$` subscriptions rather than the design's first/last state reader, and the README's two-mount sharing claim omits the independently created column runtimes. | Align the lifetime contract with the design or document the explicit subscription contract precisely after fixing column-runtime sharing. |
| smell | `packages/grid/src/11_treeTableRow.tsx:78`; `packages/grid/src/11_treeTableRow.tsx:107`; `packages/grid/src/15_treeTable.browser.test.tsx:156` | Shrinking the indent cannot shrink the 14px button or preceding 6px gap, so a tree cell narrower than its padding plus those fixed widths can clip the twisty even though `overflow: hidden` prevents painting into the adjacent column. | Enforce a tree-column minimum that reserves the twisty and gaps, and test the minimum actual cell width as well as depth 12 in a 120px column. |
| smell | `packages/grid/src/16_epics.test.ts:84`; `packages/grid/src/15_treeTable.browser.test.tsx:63`; `packages/vitest-telemetry/tests/report.e2e.test.ts:102`; `packages/boop-adapters/tests/network.e2e.test.ts:79` | Grid unit coverage recreates the pivot epic manually, grid browser coverage does not exercise Alt-click or recursive header sorting, and only telemetry has a DOM pivot test whose assertions count the stack rather than matching rows. | Exercise the exported helper through DOM events and `useGridEffect` in both consumers, asserting one matching pivot, unchanged selection, recursive sort order, and teardown across two mounts. |
| smell | `packages/signals/src/10_slice.ts:57`; `packages/grid/src/2_createGrid.ts:24`; `packages/grid/src/2_createGrid.ts:45`; `packages/grid/src/9_treeSize.ts:5`; `packages/grid/src/10_treeColumn.ts:39`; `packages/grid/src/10_treeColumn.ts:55`; `packages/grid/src/10_treeColumn.ts:65`; `packages/grid/src/10_treeColumn.ts:76`; `packages/grid/src/11_treeTableRow.tsx:7` | Shared helpers occupy factory, type, and component files outside `src/lib`, and `10_treeColumn.ts` combines conversion, receivers, modifiers, and visibility concerns. | Move each shared concern into its package's numbered `src/lib` file with an adjacent test and retain public re-exports. |
| smell | `packages/grid/src/7_compactChains.ts:26`; `packages/grid/src/8_pivot.ts:12`; `packages/boop-adapters/src/report-app/nav.ts:31`; `packages/boop-adapters/src/report-app/nav.ts:44`; `packages/report-shell/src/sizingRestore.ts:65` | Shared tree, row-shaping, and sizing helpers also remain outside package-level `src/lib`, with boop row shaping combining separate concerns without adjacent tests. | Move the helpers to concern-specific package `src/lib` modules and colocate their tests. |
| smell | `packages/boop-adapters/src/report-app/components/App.tsx:23`; `packages/vitest-telemetry/src/report-app/components/App.tsx:24`; `packages/boop-adapters/src/report-app/model.ts:24`; `packages/vitest-telemetry/src/report-app/model.ts:59` | Theme/density binding and pivot-stack serialization are duplicated across the two report consumers. | Move these concerns into report-shell as listed in the reuse table. |
| nit | `packages/vitest-telemetry/src/report-app/components/Nav.tsx:14` | `lastOf` wraps an array reduction solely to return its last element. | Read the last array element directly at the call site. |
| nit | `packages/grid/src/1_types.ts:49`; `packages/grid/src/2_createGrid.ts:131`; `packages/grid/src/3_react.ts:8`; `packages/grid/src/10_treeColumn.ts:9`; `packages/grid/src/10_treeColumn.ts:26`; `packages/grid/src/10_treeColumn.ts:64`; `packages/grid/src/10_treeColumn.ts:75`; `packages/grid/src/12_treeTable.tsx:31`; `packages/signals/src/10_slice.ts:5`; `packages/signals/src/10_slice.ts:20`; `packages/signals/src/10_slice.ts:29`; `packages/signals/src/10_slice.ts:33`; `packages/signals/src/10_slice.ts:56` | These comments narrate operations already expressed by adjacent types or code instead of stating otherwise invisible constraints. | Delete the narration and retain only necessary constraints in at most two consecutive lines. |
| nit | `packages/boop-adapters/src/report-app/components/App.tsx:1`; `packages/boop-adapters/src/report-app/model.ts:1`; `packages/boop-adapters/src/report-app/nav.ts:1`; `packages/vitest-telemetry/src/report-app/components/App.tsx:1`; `packages/vitest-telemetry/src/report-app/components/Nav.tsx:1`; `packages/vitest-telemetry/src/report-app/components/Nav.tsx:38`; `packages/vitest-telemetry/src/report-app/components/NavColumns.tsx:1`; `packages/vitest-telemetry/src/report-app/components/NavColumns.tsx:19`; `packages/vitest-telemetry/src/report-app/components/NavColumns.tsx:40`; `packages/vitest-telemetry/src/report-app/lib/navRoots.ts:1` | Consumer comments narrate layout, implementation history, or directly visible operations, including the stale claim that boop still uses `NavGrid`. | Remove narrative/history comments and retain only constraints absent from the code. |
| nit | `packages/grid/README.md:218`; `packages/grid/README.md:298`; `packages/grid/README.md:299`; `packages/grid/README.md:301`; `packages/grid/README.md:307`; `packages/grid/README.md:308`; `packages/grid/README.md:309`; `packages/grid/README.md:328`; `packages/grid/README.md:337`; `packages/grid/README.md:350`; `packages/grid/README.md:353`; `packages/signals/README.md:34`; `packages/signals/README.md:39`; `packages/signals/README.md:43`; `packages/signals/README.md:46`; `packages/signals/README.md:90` | These README lines contain prohibited em dash characters, including retained text. | Replace prose separators with punctuation and table placeholders with `none` or `required`. |
| nit | `packages/grid/src/4_grid.tsx:32`; `packages/grid/src/5_grid.browser.test.tsx:174`; `packages/grid/src/5_grid.browser.test.tsx:910`; `packages/grid/src/5_grid.browser.test.tsx:914` | Existing grid files retain em dashes in comments and a repository teardown local named `dispose`. | Replace the comment punctuation and expose/call the teardown as `unsubscribe`. |
| nit | `packages/marbler/src/2_marbler.css:632` | These comment blocks exceed two consecutive lines. | Keep only otherwise invisible constraints and limit each block to two lines. |
| nit | `packages/report-shell/src/sizing.test.ts:5` | These comment blocks exceed two consecutive lines. | Keep only otherwise invisible constraints and limit each block to two lines. |
| nit | `packages/report-shell/src/style.css:108` | These comment blocks exceed two consecutive lines. | Keep only otherwise invisible constraints and limit each block to two lines. |
| nit | `packages/signals/src/3_react.ts:50`; `packages/signals/src/3_react.ts:87` | These comment blocks exceed two consecutive lines. | Keep only otherwise invisible constraints and limit each block to two lines. |
| nit | `packages/vitest-telemetry/fixtures/vite.app.config.ts:4` | These comment blocks exceed two consecutive lines. | Keep only otherwise invisible constraints and limit each block to two lines. |
| nit | `packages/vitest-telemetry/fixtures/vitest.config.ts:5` | These comment blocks exceed two consecutive lines. | Keep only otherwise invisible constraints and limit each block to two lines. |
| nit | `packages/vitest-telemetry/src/cli.ts:59`; `packages/vitest-telemetry/src/cli.ts:102`; `packages/vitest-telemetry/src/cli.ts:113`; `packages/vitest-telemetry/src/cli.ts:155`; `packages/vitest-telemetry/src/cli.ts:175` | These comment blocks exceed two consecutive lines. | Keep only otherwise invisible constraints and limit each block to two lines. |
| nit | `packages/vitest-telemetry/src/index.ts:4` | These comment blocks exceed two consecutive lines. | Keep only otherwise invisible constraints and limit each block to two lines. |
| nit | `packages/vitest-telemetry/src/otel.node.ts:1`; `packages/vitest-telemetry/src/otel.node.ts:19` | These comment blocks exceed two consecutive lines. | Keep only otherwise invisible constraints and limit each block to two lines. |
| nit | `packages/vitest-telemetry/src/plugin.ts:60` | These comment blocks exceed two consecutive lines. | Keep only otherwise invisible constraints and limit each block to two lines. |
| nit | `packages/vitest-telemetry/src/receiver.ts:1` | These comment blocks exceed two consecutive lines. | Keep only otherwise invisible constraints and limit each block to two lines. |
| nit | `packages/vitest-telemetry/src/report-app/adapter/hostPid.ts:1` | These comment blocks exceed two consecutive lines. | Keep only otherwise invisible constraints and limit each block to two lines. |
| nit | `packages/vitest-telemetry/src/report-app/adapter/navTree.ts:1`; `packages/vitest-telemetry/src/report-app/adapter/navTree.ts:109` | These comment blocks exceed two consecutive lines. | Keep only otherwise invisible constraints and limit each block to two lines. |
| nit | `packages/vitest-telemetry/src/report-app/components/PresetsMenu.tsx:1` | These comment blocks exceed two consecutive lines. | Keep only otherwise invisible constraints and limit each block to two lines. |
| nit | `packages/vitest-telemetry/src/report-app/model.ts:1`; `packages/vitest-telemetry/src/report-app/model.ts:81` | These comment blocks exceed two consecutive lines. | Keep only otherwise invisible constraints and limit each block to two lines. |
| nit | `packages/vitest-telemetry/src/report-app/prefs.ts:1` | These comment blocks exceed two consecutive lines. | Keep only otherwise invisible constraints and limit each block to two lines. |
| nit | `packages/vitest-telemetry/src/report-app/style.css:32`; `packages/vitest-telemetry/src/report-app/style.css:53` | These comment blocks exceed two consecutive lines. | Keep only otherwise invisible constraints and limit each block to two lines. |
| nit | `packages/vitest-telemetry/src/report/chrome-trace.ts:1` | These comment blocks exceed two consecutive lines. | Keep only otherwise invisible constraints and limit each block to two lines. |
| nit | `packages/vitest-telemetry/src/report/timeline.ts:27`; `packages/vitest-telemetry/src/report/timeline.ts:543` | These comment blocks exceed two consecutive lines. | Keep only otherwise invisible constraints and limit each block to two lines. |
| nit | `packages/vitest-telemetry/tests/report.e2e.test.ts:1` | These comment blocks exceed two consecutive lines. | Keep only otherwise invisible constraints and limit each block to two lines. |
| nit | `packages/vitest-telemetry/tests/report.shell.e2e.test.ts:1` | These comment blocks exceed two consecutive lines. | Keep only otherwise invisible constraints and limit each block to two lines. |

| From file | To package | Symbol |
| --- | --- | --- |
| `packages/boop-adapters/src/report-app/components/App.tsx:23`; `packages/vitest-telemetry/src/report-app/components/App.tsx:24` | `@hafley66/report-shell`, `src/lib` | Shared theme/density subscription from `useTheme`, leaving telemetry-specific column attributes in its consumer. |
| `packages/boop-adapters/src/report-app/model.ts:24`; `packages/vitest-telemetry/src/report-app/model.ts:59` | `@hafley66/report-shell`, `src/lib` | `encodePivotStack`, `decodePivotStack`, and the history-backed pivot-stack construction. |
| `packages/boop-adapters/src/report-app/model.ts:117`; `packages/vitest-telemetry/src/report-app/model.ts:147` | `@hafley66/report-shell`, same pivot-stack concern | `pushPivot`, `popPivotsTo`. |
| `packages/boop-adapters/src/report-app/components/App.tsx:75`; `packages/vitest-telemetry/src/report-app/components/Nav.tsx:64` | `@hafley66/report-shell`, same pivot-stack concern | Pivot effect to `PivotEntry` binding, using `effect.column` and a supplied value formatter. |
| `packages/boop-adapters/src/report-app/model.ts:88`; `packages/vitest-telemetry/src/report-app/model.ts:134` | `@hafley66/report-shell`, `src/lib` | Marbler source/viewport synchronization on selection changes, with a shared RxJS lifetime and a supplied event projection. |
| `packages/boop-adapters/src/report-app/components/App.tsx:71`; `packages/vitest-telemetry/src/report-app/components/App.tsx:42` | `@hafley66/report-shell` | Repeated layout, nav rail, gutter refs, and gutter subscription in a report layout component accepting tracks, storage keys, and content. |
| `packages/boop-adapters/src/report-app/components/sessionColumns.tsx:6`; `packages/vitest-telemetry/src/report-app/components/NavColumns.tsx:26`; `packages/boop-adapters/src/report-app/style.css:25`; `packages/vitest-telemetry/src/report-app/style.css:58` | `@hafley66/report-shell` | Shared primary/secondary name-cell presentation, keeping domain label generation in each consumer. |

- Initial `git apply --index` exited 1 because the patch omitted a binary payload; remaining hunks applied with exit 0 and `timelineToMarble.ts` was restored from exact local blob `b71c214`.
- Missing worktree dependencies prevented passing receipts; installs were skipped as instructed, and no source fixes or commits were made.
- Static trace: row click (`11_treeTableRow.tsx:43`) -> own `column$` (`10_treeColumn.ts:58`) -> `pivotOnAltClick` (`10_treeColumn.ts:76`) -> feedback (`10_slice.ts:65`) -> `useGridEffect` (`3_react.ts:15`) -> boop `App.tsx:81` / telemetry `Nav.tsx:28`.
- Both target consumers have 0 `altKey`/`stopPropagation` matches; the retained report-shell `NavGrid.tsx:86` still implements its own Alt-click path.
- Column context supplies own `column$`, all three `phase$` streams, `grid$`, `state`, `rows`, and `dispatch`; DOM geometry and end-to-end behavior remain unverified at runtime because the receipt chains failed.
