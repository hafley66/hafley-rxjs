{
  "name": "@hafley66/grapht-layout-grid-wasm",
  "version": "0.1.0",
  "description": "Deterministic Rust/Wasm grid layout executed in a worker",
  "type": "module",
  "main": "./dist/index.js",
  "types": "./dist/index.d.ts",
  "bin": { "grapht-layout-grid-wasm": "./bin/layout-grid-wasm.mjs" },
  "files": ["dist", "bin", "src", "test", "scripts", "wasm", "LEARNINGS.md"],
  "scripts": {
    "build:wasm": "cargo build --manifest-path Cargo.toml --target wasm32-unknown-unknown --release && mkdir -p wasm && cp target/wasm32-unknown-unknown/release/grapht_layout_wasm.wasm wasm/grapht_layout_wasm.wasm",
    "build": "pnpm build:wasm && tsc -p tsconfig.json",
    "typecheck": "tsc --noEmit -p tsconfig.json",
    "test": "node --test test/*.test.mjs",
    "receipt": "node scripts/receipt.mjs"
  },
  "devDependencies": { "@types/node": "^26.2.0", "typescript": "7.0.2" }
}
