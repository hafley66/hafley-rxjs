// Own entry, absent from the plugins index: a host that never imports this subpath never has its
// bundler resolve the renderer chunk (signal-grid tree mode).
export { fsTreePlugin } from "./2_fsTreePlugin.js";
