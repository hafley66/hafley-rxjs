// Own entry, absent from the plugins index: a host that never imports this subpath never has its
// bundler resolve the optional peer `@hafley66/signal-marbles`.
export { marblesPlugin } from "./2_marblesPlugin.js";
