// Own entry, absent from the plugins index: a host that never imports this subpath never has its
// bundler resolve the optional peer `echarts`.
export { echartsPlugin } from "./2_echartsPlugin.js";
